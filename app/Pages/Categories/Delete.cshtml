@page "{id:int}"
@model ExpenseApp.Pages.Categories.DeleteModel
@{
    ViewData["Title"] = "Deactivate Category";
}

<div class="top-bar">
    <h5 class="mb-0 fw-bold text-warning">Deactivate Category</h5>
    <a asp-page="Index" class="btn btn-outline-secondary btn-sm">← Back</a>
</div>

<div class="row justify-content-center">
    <div class="col-md-5">
        <div class="card border-warning">
            <div class="card-header bg-warning">Confirm</div>
            <div class="card-body">
                <p>Deactivate category <strong>@RouteData.Values["id"]</strong>?</p>
                <form method="post">
                    <input type="hidden" name="id" value="@RouteData.Values["id"]" />
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-warning">Deactivate</button>
                        <a asp-page="Index" class="btn btn-outline-secondary">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
